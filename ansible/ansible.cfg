[defaults]
# Silence the Python interpreter auto-discovery warning
interpreter_python = /usr/bin/python3

# Opt in to the new fact-access style now so the playbook is ready for
# ansible-core 2.24 when inject_facts_as_vars defaults to False
inject_facts_as_vars = False
